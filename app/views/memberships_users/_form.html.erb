<%= form_with(model: memberships_user, local: true) do |form| %>
  <% if memberships_user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(memberships_user.errors.count, "error") %> prohibited this memberships_user from being saved:</h2>

      <ul>
        <% memberships_user.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :user_id %>
    <%= form.text_field :user_id %>
  </div>

  <div class="field">
    <%= form.label :membership_id %>
    <%= form.text_field :membership_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
